function toogleMenu() {
    if ($( "#transition-menu" ).hasClass("show")) {
      $( "#transition-menu" ).removeClass("show");
    }
    else {
      $( "#transition-menu" ).addClass("show");
    }
}
